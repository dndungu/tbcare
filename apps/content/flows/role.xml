<?xml version="1.0" encoding="UTF-8"?>
<flow name="role">
    <select access="permission.roles"></select>
    <insert access="permission.roles.insert"></insert>
    <update access="permission.roles.update"></update>
    <delete access="permission.roles.delete"></delete>
</flow>